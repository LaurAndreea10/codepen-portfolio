# Google Search Console setup

Property to add:

`https://laurandreea10.github.io/codepen-portfolio/`

## After verification

1. Submit `https://laurandreea10.github.io/codepen-portfolio/sitemap.xml`.
2. Inspect and request indexing for `/`, `/en/`, `/alpis-fusion-crm.html`, `/projects/clientflow.html`, and `/projects/clientops-suite-premium.html`.
3. Check Page indexing for redirects, duplicates and “Crawled — currently not indexed”.
4. Review Performance monthly: queries, impressions, clicks, CTR and average position.
5. Revalidate structured data after major template changes.

GitHub Pages does not allow uploading a verification file outside this repository. If Google provides an HTML verification filename, add that exact file to the repository root and deploy it before clicking Verify.
